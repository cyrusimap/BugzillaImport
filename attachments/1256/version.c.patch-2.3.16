--- imap/version.c	2010-01-30 15:40:56.000000000 -0600
+++ imap/version.c	2010-01-30 15:51:50.000000000 -0600
@@ -146,7 +146,7 @@
 	     "; Running w/%s", SSLeay_version(SSLEAY_VERSION));
 #ifdef EGD_SOCKET
     snprintf(env_buf + strlen(env_buf), MAXIDVALUELEN - strlen(env_buf),
-	     " (with EGD)")
+	     " (with EGD)");
 #endif
 #endif
 #ifdef HAVE_ZLIB
